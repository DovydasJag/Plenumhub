// The one place to change the name, launch date, address and contact email.
// After changing name, siteUrl or contactEmail, run `npm run apply-config` so the
// static pages, meta tags, sitemap and share image pick it up. `npm test` checks this.
export const CONFIG = {
  name: "Plenum",
  // Puzzle No. 1 is shown on this date (the player's local calendar date).
  launchDate: "2026-09-25",
  siteUrl: "https://plenumhub.com",
  contactEmail: "dovydasjagm@gmail.com",
  // AdSense publisher ID. Its script goes in every page's <head>; leave empty for no AdSense at all.
  adsenseClient: "ca-pub-7152450473146090",
  description: "A daily word puzzle. One pool of letters, three categories. Use as many tiles as you can.",
};
